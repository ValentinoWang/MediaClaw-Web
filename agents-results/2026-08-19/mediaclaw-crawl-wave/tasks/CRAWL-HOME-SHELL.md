# CRAWL-HOME-SHELL

Read `CRAWL-PROTOCOL.md` first. Crawl exactly `/` and `/en`. Capture the shared header/footer destination links and the page-level title, language, headings, landmarks, sections, forms, and internal links. Write only `artifacts/CRAWL-HOME-SHELL.json` and task-local logs/scripts under the crawl-wave directory. Do not modify `src/**`, package files, dist, or another task artifact. Use the fixed return path supplied by the supervisor and report `VERIFIED` only after the task-local validation passes.
