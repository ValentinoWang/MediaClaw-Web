# CRAWL-BLOG-INDEX

Read `CRAWL-PROTOCOL.md` first. Crawl exactly `/blog`, `/en/blog`, the twelve Chinese category paths from `PAGE_CLASSIFICATION.md`, and the twelve English category paths from that file. Preserve percent-encoded Chinese paths in the artifact and record decoded labels separately. Capture list counts, article href sets, category navigation, title, headings, landmarks, and aside presence. Write only `artifacts/CRAWL-BLOG-INDEX.json` and task-local logs/scripts. Do not modify source code or other artifacts.
